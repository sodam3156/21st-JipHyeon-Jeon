---
document_id: repo.ai-instructions
status: active
owner_role: product-owner
last_updated: 2026-07-24
---

# AI 협업 지침

이 파일은 이 저장소에서 작업하는 모든 AI 도구와 사람에게 적용됩니다. 사용자 지시가 이 문서와 충돌하면 사용자 지시를 우선하되, 충돌과 영향 범위를 결과에 밝힙니다.

## 1. 작업 전 읽기 순서

1. `AGENTS.md`
2. `docs/DOCUMENT_INDEX.md`
3. `docs/00_overview/CURRENT_STATUS.md`
4. `docs/07_decisions/GATE_LOG.md`
5. 요청과 관련된 정본 문서

상태·근거·게이트·고객·문제·아이디어·P0를 바꾸는 Full gate 작업은 `docs/00_overview/INSIGHT_LEDGER.md`, `docs/00_overview/OPPORTUNITY_MAP.md`, `docs/04_validation/EVIDENCE_LOG.md`, `docs/07_decisions/GATE_LOG.md`, `docs/07_decisions/DECISION_LOG.md`를 추가로 읽습니다. 아이디어·솔루션 비교는 `docs/03_solution/IDEA_PORTFOLIO.md`와 `docs/03_solution/SOLUTION_PORTFOLIO.md`도 읽습니다. AI 에이전트 실행은 `docs/08_ai/AGENT_FRAMEWORK.md`와 `docs/08_ai/AGENT_TASK_QUEUE.md`도 읽습니다.

문서 경로를 이름으로 추측하지 말고 문서 ID로 정본을 찾습니다. 정본을 찾지 못하면 임의 경로에 대체 문서를 만들지 말고 작업을 중단해 누락을 보고합니다.

## 2. 현재 단계

- 현재 단계는 **인사이트 초안의 사람 승인 대기와 기회 영역 비교**입니다.
- 메인 아이디어는 아직 선택되지 않았습니다.
- `회동(會同)`은 `IDEA-001`, `seed` 상태의 초기 메모입니다.
- 회동의 이전 솔루션 문서는 archived이고 PRD는 선택 전까지 의도적으로 비어 있습니다.
- `D-006`에 따라 Gate 5와 사람의 결정 없이 어떤 후보도 `selected`로 표현하지 않습니다.

## 3. 정보 상태

기획 문장을 만들거나 수정할 때 다음 상태를 구분합니다.

- **사실**: 직접 확인 가능한 정보
- **근거**: 인터뷰, 관찰, 통계, 원문, 테스트 결과
- **관찰**: 원천에서 실제 확인한 사실 또는 주장
- **인사이트**: 복수 관찰을 해석해 앞으로의 선택을 바꾸는 제약
- **가설**: 아직 검증되지 않은 설명이나 예측
- **제안**: AI 또는 팀원이 검토를 위해 만든 선택지
- **결정**: 책임자가 선택해 현재 적용 중인 방향
- **검증 결과**: 사전 기준에 따라 실행한 테스트 결과
- **미정**: 선택 또는 검증이 필요한 항목

가설을 사실로 바꾸거나, 인터뷰하지 않은 말을 고객 발언처럼 만들거나, 출처를 추정해 채우지 않습니다.

## 4. 근거 사슬

모든 아이디어·솔루션 제안은 다음 연결을 유지합니다.

핵심 계보는 `원천 RS·근거 E·세종 문제 SJ-P → 관찰 OBS → 인사이트 IN → 기회 OPP → 아이디어 IDEA → 솔루션 SOL → 결정 D`입니다.

`HYP`는 OPP·IDEA·SOL에 연결하는 반증 가능한 가설입니다. 각 대상은 승격 전에 최근 행동 근거 `E` 또는 사전 기준을 고정한 실험 `X`로 치명 가설을 검증합니다.

- 연결이 끊긴 아이디어는 seed를 넘어 포트폴리오로 승격하지 않습니다.
- 같은 주장을 표현만 바꿔 새 인사이트로 만들지 않습니다.
- 지지 근거와 반박 근거를 함께 기록합니다.
- 아이디어를 살리기 위해 인사이트의 의미나 신뢰도를 바꾸지 않습니다.

## 5. 원천의 용도 경계

- `sejong-ax.problem-bank`는 세종 문제 후보와 원출처를 찾는 자료입니다. 개별 문제의 등급과 최신성을 확인합니다.
- 해커톤 전략 6개 문서는 아이디어 선택·데모·운영 제약의 보조 근거입니다. 특정 고객 문제를 증명하지 않습니다.
- 백엔드 실행 10개 문서는 계약·평가·권한·예산·복구의 기술 안전장치로만 사용합니다. 시장성이나 문제 강도의 근거가 아닙니다.
- 영상 수, 문서 수, 모델 간 합의는 근거의 강도를 뜻하지 않습니다.

원천 등록과 한계는 `validation.research-source-catalog`, 고객·솔루션 근거는 `validation.evidence-log`에 기록합니다.

## 6. 정본과 변경 규칙

- 문서 ID마다 정본은 하나만 유지합니다.
- “최종”, “수정본”, “v2” 같은 중복 파일을 만들지 않습니다.
- 새 문서를 만들기 전에 `repo.document-index`에서 같은 목적의 문서를 찾습니다.
- 주요 고객, 핵심 문제, 가치 제안, 선택 아이디어, MVP P0 범위가 바뀌면 아래를 한 Pull Request에서 함께 갱신합니다.
  1. `planning.current-status`
  2. 해당 문제·고객·아이디어·솔루션 정본
  3. `governance.decision-log`
  4. 영향을 받는 PRD·일정·데모 문서
- 삭제보다 상태 변경과 결정 기록을 우선합니다.
- 외부 근거에는 URL 또는 원천 ID, 확인일, 실제 확인한 주장, 한계를 남깁니다.

## 7. 기획 판단 순서

항상 다음 순서로 판단합니다.

1. 인사이트와 문제 ID
2. 구체 고객
3. 장소·촉발 시각이 있는 특정 상황
4. 반복되는 실패 행동과 측정 가능한 손실
5. 기존 대안과 미충족 이유
6. AI가 바꾸는 판단 또는 남기는 업무 결과물
7. 필요한 데이터와 결측·오류 처리
8. 해커톤 기간에 구현 가능한 대표 사건 하나
9. 3분 발표 안의 90초 핵심 데모 상태 변화
10. 반증 실험과 사람 결정

고객과 문제 검증 없이 기능 목록부터 확장하지 않습니다.

## 8. 아이디어·솔루션 경계

- 관광객, 이주민, 대학생처럼 서로 다른 고객을 동시에 주요 고객으로 잡지 않습니다.
- 세종 전체 관광·생활·부동산·의료를 포괄하는 통합 정보 서비스로 확장하지 않습니다.
- AI가 버스·의료진·돌봄 인력·문화시설 같은 공급을 만든다고 주장하지 않습니다.
- “여러 AI가 토론한다”는 표현은 사용자 가치가 아닙니다. 독립된 책임·데이터·검증 기준이 있을 때만 역할을 분리합니다.
- MVP는 완전 자율 멀티에이전트보다 하나의 제어 가능한 흐름, 검증된 데이터, 명확한 수용 기준을 우선합니다.
- 캐릭터·집현전 세계관은 이해와 몰입을 돕는 표현 계층이며 문제 해결을 방해하면 축소합니다.
- mock·합성·사전 입력 데이터는 화면과 문서에서 실제 연동과 구분합니다.

## 9. AI 에이전트 실행

에이전트는 `ai.agent-framework`의 게이트와 역할 계약을 따릅니다.

- `ai.agent-task-queue`에서 `ready`인 AR-ID로 시작합니다.
- 한 실행은 질문 하나와 단계 하나만 처리합니다.
- 작업 전 도구·재시도·시간·비용·후보 수·권한 상한을 고정합니다.
- AR-003에서 선택하고 Gate 2를 통과한 두 기회 영역에서 영역별 최대 2개 아이디어만 생성합니다.
- 생성과 반론은 독립된 세션 또는 에이전트에서 수행합니다.
- 실행 후에는 설명이 아니라 파일 재조회·테스트·실제 경로로 완료를 증명합니다.
- Gate를 통과하지 못하면 부분 성공 또는 차단으로 기록하고 다음 단계로 진행하지 않습니다.

## 10. 역할별 기대 결과

### 조사

- 최신성이 필요한 사실은 공식 원문을 우선합니다.
- 실제로 읽은 범위와 읽지 못한 범위를 구분합니다.
- 검색 결과 요약이나 메타데이터만으로 강한 결론을 내리지 않습니다.
- claim 수준의 의미 중복을 검사합니다.

### 기획

- 고객·상황·실패 행동·손실을 분해합니다.
- 반증 가능한 가설과 48시간 검증 방법을 함께 제시합니다.
- 인터뷰는 해결책 호감보다 최근 실제 행동을 묻습니다.
- 사용자·수혜자·구매자·운영자·도입 결정자를 구분합니다.

### 개발

- 구현 전 선택된 아이디어, PRD, 수용 기준을 확인합니다.
- API·데이터·LLM 출력 계약을 구조화하고 런타임에서 검증합니다.
- P0 밖의 기능을 임의로 추가하지 않습니다.
- timeout, 429, 형식 오류, 오래된 데이터, API 장애와 오프라인 축소 모드를 처리합니다.
- tool call 성공이나 배포 상태만으로 완료 처리하지 않고 사용자 경로를 다시 검사합니다.

### 평가

- 후보에 같은 입력·시간·데이터·평가표를 적용합니다.
- 축별 근거 등급과 불확실성·탈락 조건을 기록하고 숫자 총점으로 자동 선택하지 않습니다.
- 모델 평가를 사용자 검증이나 실제 심사 결과처럼 표현하지 않습니다.
- AI는 추천할 수 있지만 메인 아이디어를 선택할 수 없습니다.

## 11. 권한과 안전

AI가 사람 승인 없이 할 수 있는 일:

- 읽기, 비교, 조사, 초안, 코드 패치, 로컬 테스트
- 별도 브랜치와 draft PR 제안

사람 승인 후에만 할 수 있는 일:

- main 병합과 운영 배포
- 외부 메시지·기관 제출
- 실제 개인정보·민감정보 사용
- 비용 상한·도구 권한·외부 전송 범위 확대
- 비가역 변경과 데이터 마이그레이션
- 메인 아이디어 `selected` 승격

비밀값, 개인정보, 공개 불가 자산은 저장소·로그·데모 데이터에 넣지 않습니다.

## 12. Pull Request 결과 형식

모든 AI 작업 결과에는 다음을 포함합니다.

- 실행 ID와 답한 질문
- 무엇을 바꿨는가
- 어떤 원천·인사이트·기회·결정에 따른 것인가
- 반박 근거와 남은 위험
- 어떤 문서 ID를 수정했는가
- 실행한 검증과 사후 상태
- 실패·부분 성공·예산 사용
- 사람의 결정이 필요한 항목

문서 변경 후 `node scripts/validate-planning-docs.mjs`를 실행하고 결과를 PR에 기록합니다.

main 브랜치에 직접 반영하지 말고, 사람이 검토할 수 있는 작은 Pull Request를 기본으로 합니다.
