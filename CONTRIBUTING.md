---
document_id: repo.collaboration-guide
status: active
owner_role: product-owner
last_updated: 2026-07-24
---

# 협업 가이드

기획자·개발자·디자이너와 각자가 사용하는 AI가 같은 근거와 상태를 보도록 하는 규칙입니다.

## 가장 먼저 확인할 것

1. `repo.ai-instructions`
2. `repo.document-index`
3. `planning.current-status`
4. `governance.gate-log`
5. 작업 대상 정본

상태·근거·게이트·고객·문제·아이디어·P0를 바꾸는 작업은 `planning.insight-ledger`, `planning.opportunity-map`, `governance.gate-log`, `governance.decision-log`도 확인합니다.

`status: active`는 문서가 현재 사용되는 정본이라는 뜻이지, 문서 안의 모든 가설이 승인됐다는 뜻이 아닙니다. 내용의 선택 상태는 `selection_status`와 accepted D-ID로 판단합니다.

## 작업 계보

모든 주요 제안은 아래 연결을 유지합니다.

핵심 계보는 `RS/E/SJ-P → OBS → IN → OPP → IDEA → SOL → D`입니다. OPP·IDEA·SOL의 치명 가설 `HYP`는 상태 승격 전에 근거 `E` 또는 실험 `X`로 검증합니다.

- 원천·근거 없이 인사이트를 만들지 않습니다.
- 인사이트 없이 문제·기회 영역을 확정하지 않습니다.
- Gate 2를 통과하지 않은 영역에서 아이디어를 대량 생성하지 않습니다.
- accepted 선택 D-ID와 `selected_idea_id`가 일치하기 전에는 솔루션·PRD를 구현 기준으로 사용하지 않습니다.
- AI는 후보를 추천할 수 있지만 `selected`로 승격할 수 없습니다.

## 역할

| 역할 ID | 담당자 | 주 책임 |
|---|---|---|
| H-PO | 백서준 | 팀장, PM, 협업 조율, 백엔드 서브, 방향·범위 최종 결정 |
| H-PQ | 손시우 | QA, 콘텐츠 기획, 페르소나·프롬프트, 고객·문제·사용성 검증 |
| H-BS | 최영 | 메인 백엔드·AI, API·DB, 개인정보 최소수집, 세션·보안 |
| H-FU | 여하윤 | UI/UX, 프론트엔드, 토론 뷰·체크리스트·리더보드 |
| H-VD | 김경은 | 캐릭터·비주얼, 웹 레이아웃·오브제 배치 |
| AI 도구 | - | 조사·초안·비교·반론·검증 보조. 인간 책임과 최종 의사결정권을 대체하지 않음 |

## Issue에서 시작하기

- AI 실행은 `AI 에이전트 실행` 양식을 사용하고 AR-ID·질문 하나·입력 ID·예산·게이트를 적습니다.
- 고객·문제·솔루션 가설 검증은 `가설 검증` 양식을 사용합니다.
- 구현·기획·디자인 작업은 `실행 작업` 양식을 사용합니다.
- 사람 인터뷰나 기관 확인이 필요한 작업은 AI가 임의로 완료 처리하지 않습니다.

## 브랜치와 Pull Request

- `planning/주제`: 인사이트·기회·고객·문제
- `research/주제`: 인터뷰·공식 원문·경쟁 대안
- `feat/기능`: 선택된 P0 기능
- `fix/문제`: 버그
- `docs/주제`: 문서·운영 규칙

한 브랜치에는 질문 하나 또는 결과물 하나를 담습니다. main에 직접 반영하지 않고 draft PR로 검토합니다.

PR에는 다음을 적습니다.

- 실행 ID와 적용 게이트
- 근거 사슬과 반박 근거
- 수정한 문서 ID
- 후보의 상태 변화와 관련 D-ID
- 실제 실행한 검증과 사후 상태
- 실패·부분 성공·남은 위험

## 인간 책임과 승인

역할별 실행 책임, 승인 권한, 상호 검토, 차단권과 부재 시 동작은 `ai.human-authority-matrix`를 정본으로 사용합니다. B–E 등급 승인 요청은 `template.approval-packet`을 사용하며, 응답이 없으면 자동 진행하지 않습니다.

## 사람 승인이 필요한 변경

- 메인 아이디어 `selected` 승격
- 주요 고객·핵심 문제·P0 변경
- main 병합과 운영 배포
- 외부 메시지·기관 제출
- 개인정보·민감정보 사용
- 비용·도구 권한·외부 전송 범위 확대
- 비가역 변경과 데이터 마이그레이션

## 완료 정의

- 정본과 실행 기록에 반영됐습니다.
- 사실·관찰·인사이트·가설·제안·결정·검증 결과가 구분됐습니다.
- 지지와 반박 근거가 연결됐습니다.
- 중복·모순·끊어진 ID 연결이 없습니다.
- 파일·API·배포 변경은 재조회나 실제 경로 테스트로 확인했습니다.
- `node scripts/validate-planning-docs.mjs`가 통과했습니다.
- 다음 작업과 사람의 결정 질문이 명확합니다.
